import styled from "styled-components";

const StyledKeyboard = styled.div`
  display: grid;
  grid-template-columns: repeat(8, auto);
  gap: 8px;
  width: fit-content;
  margin: 0 auto;
`;

export { StyledKeyboard };
